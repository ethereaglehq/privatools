# Tooling installed by CI itself, not by the app.
#
# Kept separate from requirements.txt (production) and requirements-dev.txt
# (test runner) because it is installed into the runner, not the image, and
# should not drag CI-only packages into either of those graphs.
#
# Regenerate the lock the same way as the others:
#   uv pip compile requirements-ci.txt --generate-hashes --universal \
#       --python-version 3.12 -o requirements-ci.lock
pip-audit==2.10.1
