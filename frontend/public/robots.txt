# PrivaTools public pages are crawlable. Private data requires authentication;
# robots.txt is a discovery preference, never an access-control boundary.
# Search assistants: OAI-SearchBot, PerplexityBot, Claude-SearchBot and ordinary
# search bots use the same public-path policy. Training agents retain the
# existing site policy; training permission is separate from search visibility.
User-agent: *
Allow: /
Disallow: /api/
Allow: /api/og-image

# Existing exclusions retained.
User-agent: ia_archiver
User-agent: Bytespider
User-agent: ImagesiftBot
User-agent: omgili
User-agent: AhrefsBot
User-agent: SemrushBot
User-agent: MJ12bot
User-agent: DotBot
Disallow: /

Sitemap: https://privatools.me/sitemap.xml

# Optional human-readable references, not required search-engine directives:
# https://privatools.me/llms.txt
# https://privatools.me/llms-full.txt
