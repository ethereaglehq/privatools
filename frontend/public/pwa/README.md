# Install screenshots

Refreshed from the running PrivaTools interface on 14 September 2026 using Chrome, without image editing. These captures include the current folded-P logo.

- Air uses the approved Morning Mist appearance.
- Play uses the approved Blush appearance.
- Desktop screenshots are 1440 × 1000; mobile screenshots are 390 × 844.
- Each capture starts in an isolated, signed-out browser context with no selected files or saved activity.
- Fonts and artwork were fully loaded. No backend warning was visible during these captures and no health responses were replaced.
- Capture details and geometry checks are recorded in `docs/brand/pwa-screenshot-refresh.json` and `docs/brand/validation.json`.

Keep these screenshots synchronized with the shipped interface and the metadata in `manifest.json`.
